<Window x:Class="WpfUI.WindowUsers"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfUI"
        mc:Ignorable="d"
        Title="WindowUsers" Height="450" Width="800">
    <Grid>
        <ListView Name="listViewUsers">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" Width="50" DisplayMemberBinding="{Binding Path=Id}"/>
                    <GridViewColumn Header="Name" Width="250" DisplayMemberBinding="{Binding Path=Name}"/>
                    <GridViewColumn Header="Login" Width="100" DisplayMemberBinding="{Binding Path=Login}"/>
                    <GridViewColumn Header="Password" Width="100" DisplayMemberBinding="{Binding Path=Password}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
